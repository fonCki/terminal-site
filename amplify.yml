version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Using pre-built artifacts from repo"
    build:
      commands:
        - echo "Build complete - using pre-built files"
  artifacts:
    baseDirectory: release/wwwroot
    files:
      - '**/*'
